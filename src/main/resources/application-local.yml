spring:
  datasource:
    url: jdbc:mysql://localhost:3306/stayInvestDb?allowPublicKeyRetrieval=true
    username: stayInvest
    password: Admin
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect